F90 ?= gfortran -ffree-line-length-none

$(info F90 Compiler Version: $(shell $(F90) --version))

hello: hello.o
	 ${F90} -o hello.x  hello.o 

# Here are the compile steps

hello.o:hello.f90
	 ${F90} -c  hello.f90

clean:
	 rm -f -r *.o *.mod *.x

